# Copy to .env and edit. CUSTOM_USER/PASSWORD gate the LOCAL KasmVNC web UI only
# (localhost) — they are NOT your broker credentials. Never commit .env.
PUID=1000
PGID=1000
TZ=Europe/Rome
CUSTOM_USER=trader
PASSWORD=changeme
