## What and why

<!-- What this changes, and the reasoning behind it. If it fixes a specific
failure, describe the failure — that context belongs in the code comments too. -->

## How it was tested

<!-- Be concrete. For anything touching provisioning or a version pin, say whether
you tested against an empty ./config, and paste the `[mt5] imports ok:` line. -->

- [ ] Built the image locally
- [ ] Booted it from an empty `./config` and reached a working terminal
- [ ] `shellcheck mt5/entrypoint.sh` passes

## Notes

<!-- Anything a reviewer should know: trade-offs, things you're unsure about,
follow-up work you deliberately left out. -->
